services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ballot_data:/app/data   # mount directory, not a file
    restart: unless-stopped

volumes:
  ballot_data:
