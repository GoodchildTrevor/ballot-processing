services:
  web:
    build: .
    container_name: ballot_processing
    ports:
      - "8099:8000"
    env_file:
      - .env
    volumes:
      - ballot_data:/app/data
    restart: unless-stopped

volumes:
  ballot_data:
